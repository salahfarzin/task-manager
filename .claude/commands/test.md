# Run tests

Runs the full Vitest test suite in single-run mode and reports results.

```bash
npm run test:ci
```

If coverage is needed:

```bash
npm run test:coverage
```

Coverage thresholds are 80% for statements, branches, functions, and lines.
